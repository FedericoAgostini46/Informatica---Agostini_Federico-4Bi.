# MODULO M0 - ESERCIZI
## Esercizio 4 - Configurazione dell'identità in Git

### Configurazione dell'identità in Git

Le quattro impostazioni globali richieste sono state configurate e verificate con il comando: **git config --list --show-origin**


### Le impostazioni risultano:

**file:/etc/gitconfig     user.name=Fede-46**
**file:/etc/gitconfig     user.email=federico.agostini@marconirovereto.it**
**file:/etc/gitconfig     gpg.program=/.codespaces/bin/gh-gpgsign**
**file:/etc/gitconfig     init.defaultbranch=main**


Tutte e quattro le impostazioni provengono dal file di configurazione globale di Git (.**gitconfig**).
L'indirizzo email configurato coincide con quello registrato sull'account GitHub d'istituto.